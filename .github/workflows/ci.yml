# Sello CI Pipeline
# This workflow runs on every push and pull request to ensure code quality.

name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1

jobs:
  check:
    name: Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - name: Run cargo check
        run: cargo check --all-targets --all-features

  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - name: Run tests
        run: cargo test --all-features

  fmt:
    name: Format
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt
      - name: Check formatting
        run: cargo fmt --all -- --check

  clippy:
    name: Clippy
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy
      - uses: Swatinem/rust-cache@v2
      - name: Run clippy
        run: cargo clippy --all-targets --all-features -- -D warnings

  docs:
    name: Documentation
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - name: Build documentation
        run: cargo doc --no-deps --all-features
        env:
          RUSTDOCFLAGS: -D warnings

  coverage:
    name: Code Coverage
    runs-on: ubuntu-latest
    # Only need write permissions for PR comments
    permissions:
      contents: read
      pull-requests: write
    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable
        with:
          components: llvm-tools-preview

      - uses: Swatinem/rust-cache@v2

      - name: Install cargo-llvm-cov
        uses: taiki-e/install-action@cargo-llvm-cov

      - name: Generate coverage and check thresholds
        id: coverage
        run: |
          # Workspace coverage threshold (integration tests exercise all crates)
          THRESHOLD=90

          # Generate workspace-wide coverage (includes all tests exercising all crates)
          OUTPUT=$(cargo llvm-cov --workspace --all-features --json 2>/dev/null || echo '{"data":[{"totals":{"lines":{"percent":0}}}]}')

          # Extract total line coverage
          COVERAGE=$(echo "$OUTPUT" | jq -r '.data[0].totals.lines.percent // 0')
          COVERAGE_INT=$(echo "$COVERAGE" | cut -d. -f1)
          COVERAGE_DISPLAY=$(printf "%.1f" "$COVERAGE")

          # Handle null/empty
          if [ -z "$COVERAGE_INT" ] || [ "$COVERAGE_INT" = "null" ]; then
            COVERAGE_INT=0
            COVERAGE_DISPLAY="0.0"
          fi

          COMMENT_BODY="## 📊 Coverage Report\n\n"
          COMMENT_BODY+="| Metric | Value |\n"
          COMMENT_BODY+="|--------|-------|\n"
          COMMENT_BODY+="| **Total Coverage** | ${COVERAGE_DISPLAY}% |\n"
          COMMENT_BODY+="| **Threshold** | ${THRESHOLD}% |\n"

          echo "## Coverage Report" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Metric | Value |" >> $GITHUB_STEP_SUMMARY
          echo "|--------|-------|" >> $GITHUB_STEP_SUMMARY
          echo "| Total Coverage | ${COVERAGE_DISPLAY}% |" >> $GITHUB_STEP_SUMMARY
          echo "| Threshold | ${THRESHOLD}% |" >> $GITHUB_STEP_SUMMARY

          echo "Total workspace coverage: ${COVERAGE_DISPLAY}% (threshold: ${THRESHOLD}%)"

          if [ "$COVERAGE_INT" -ge "$THRESHOLD" ]; then
            COMMENT_BODY+="\n### ✅ Coverage threshold met\n"
            echo "coverage_failed=false" >> $GITHUB_OUTPUT
          else
            COMMENT_BODY+="\n### ❌ Coverage below threshold\n"
            COMMENT_BODY+="Coverage is ${COVERAGE_DISPLAY}%, need ${THRESHOLD}%.\n"
            echo "coverage_failed=true" >> $GITHUB_OUTPUT
          fi

          COMMENT_BODY+="\n<sub>Generated by CI • cargo-llvm-cov (workspace coverage)</sub>"

          echo -e "$COMMENT_BODY" > coverage_comment.md

      - name: Find existing coverage comment
        if: github.event_name == 'pull_request'
        uses: peter-evans/find-comment@v3
        id: find_comment
        with:
          issue-number: ${{ github.event.pull_request.number }}
          comment-author: 'github-actions[bot]'
          body-includes: '📊 Coverage Report'

      - name: Create or update coverage comment
        if: github.event_name == 'pull_request'
        uses: peter-evans/create-or-update-comment@v4
        with:
          comment-id: ${{ steps.find_comment.outputs.comment-id }}
          issue-number: ${{ github.event.pull_request.number }}
          body-path: coverage_comment.md
          edit-mode: replace

      - name: Fail if coverage thresholds not met
        if: steps.coverage.outputs.coverage_failed == 'true'
        run: |
          echo "::error::One or more crates failed to meet their coverage threshold"
          exit 1

  security:
    name: Security Audit
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      - uses: actions/checkout@v4

      - name: Install cargo-audit
        run: cargo install cargo-audit

      - name: Run security audit
        id: audit
        env:
          # Disable color output for cleaner parsing
          CARGO_TERM_COLOR: never
          NO_COLOR: 1
        run: |
          # Run audit and capture output (no colors for clean parsing)
          set +e
          AUDIT_OUTPUT=$(cargo audit 2>&1)
          AUDIT_EXIT=$?
          set -e

          # Strip any remaining ANSI codes just in case
          AUDIT_OUTPUT=$(echo "$AUDIT_OUTPUT" | sed 's/\x1b\[[0-9;]*m//g')

          echo "$AUDIT_OUTPUT"

          # Check for vulnerabilities in OUR crates (sello-*)
          # These are the only ones we can directly fix
          # Vulnerabilities in transitive deps from solana-sdk, bitcoin, etc. we can only warn about
          DIRECT_VULNS=$(echo "$AUDIT_OUTPUT" | grep -B20 "Solution:" | grep -E "sello(-|_)" | wc -l || echo "0")
          
          # Total vulnerabilities with solutions (includes transitive)
          TOTAL_VULNS=$(echo "$AUDIT_OUTPUT" | grep -c "Solution:" || echo "0")
          TRANSITIVE_VULNS=$((TOTAL_VULNS - DIRECT_VULNS))
          
          # Extract warning count (unmaintained, unsound without known exploit)
          WARNING_COUNT=$(echo "$AUDIT_OUTPUT" | grep -c "Warning:" || echo "0")

          # Build PR comment
          {
            echo "## 🔒 Security Audit Report"
            echo ""

            if [ "$DIRECT_VULNS" -gt 0 ]; then
              echo "### ❌ Direct Vulnerabilities Found"
              echo ""
              echo "Found **$DIRECT_VULNS vulnerability(ies)** in direct dependencies that must be fixed."
              echo ""
              echo "\`\`\`"
              echo "$AUDIT_OUTPUT" | grep -E "(Crate:|Version:|Title:|Date:|ID:|URL:|Solution:)" | head -50
              echo "\`\`\`"
              echo "has_vulnerabilities=true" >> $GITHUB_OUTPUT
            else
              echo "### ✅ No Direct Vulnerabilities"
              echo ""
              echo "has_vulnerabilities=false" >> $GITHUB_OUTPUT
            fi

            if [ "$TRANSITIVE_VULNS" -gt 0 ]; then
              echo ""
              echo "### ⚠️ Transitive Vulnerabilities ($TRANSITIVE_VULNS)"
              echo ""
              echo "The following vulnerabilities are in transitive dependencies (e.g., from solana-sdk)."
              echo "We cannot directly fix these - they require upstream updates."
              echo ""
              echo "<details>"
              echo "<summary>Click to expand transitive vulnerabilities</summary>"
              echo ""
              echo "| Crate | Advisory | Title |"
              echo "|-------|----------|-------|"
              
              echo "$AUDIT_OUTPUT" | awk '
                /^Crate:/ { crate=$2 }
                /^ID:/ { id=$2 }
                /^Title:/ { 
                  title=$0; 
                  sub(/^Title:[[:space:]]*/, "", title);
                  if (crate && id) {
                    printf "| %s | %s | %s |\n", crate, id, title
                  }
                }
                /^Solution:/ { crate=""; id=""; title="" }
              '
              
              echo ""
              echo "</details>"
            fi

            if [ "$WARNING_COUNT" -gt 0 ]; then
              echo ""
              echo "### ℹ️ Warnings ($WARNING_COUNT)"
              echo ""
              echo "Unmaintained or low-severity advisories in transitive dependencies:"
              echo ""
              echo "<details>"
              echo "<summary>Click to expand warnings</summary>"
              echo ""
              echo "| Crate | Advisory | Type | Title |"
              echo "|-------|----------|------|-------|"
              
              echo "$AUDIT_OUTPUT" | awk '
                /^Crate:/ { crate=$2 }
                /^ID:/ { id=$2 }
                /^Warning:/ { warning=$2 }
                /^Title:/ { 
                  title=$0; 
                  sub(/^Title:[[:space:]]*/, "", title);
                  if (crate && id && warning) {
                    printf "| %s | %s | %s | %s |\n", crate, id, warning, title
                  }
                  crate=""; id=""; warning=""; title=""
                }
              '
              
              echo ""
              echo "</details>"
            fi

            echo ""
            echo "<sub>Generated by CI • cargo-audit</sub>"
          } > audit_comment.md

          # Also add to step summary
          echo "## Security Audit" >> $GITHUB_STEP_SUMMARY
          if [ "$DIRECT_VULNS" -gt 0 ]; then
            echo "❌ **$DIRECT_VULNS direct vulnerabilities found**" >> $GITHUB_STEP_SUMMARY
          else
            echo "✅ No direct vulnerabilities" >> $GITHUB_STEP_SUMMARY
          fi
          if [ "$TRANSITIVE_VULNS" -gt 0 ]; then
            echo "⚠️ $TRANSITIVE_VULNS transitive vulnerabilities (from external crates)" >> $GITHUB_STEP_SUMMARY
          fi
          if [ "$WARNING_COUNT" -gt 0 ]; then
            echo "ℹ️ $WARNING_COUNT warnings (unmaintained deps, etc.)" >> $GITHUB_STEP_SUMMARY
          fi

      - name: Find existing audit comment
        if: github.event_name == 'pull_request'
        uses: peter-evans/find-comment@v3
        id: find_comment
        with:
          issue-number: ${{ github.event.pull_request.number }}
          comment-author: 'github-actions[bot]'
          body-includes: '🔒 Security Audit Report'

      - name: Create or update audit comment
        if: github.event_name == 'pull_request'
        uses: peter-evans/create-or-update-comment@v4
        with:
          comment-id: ${{ steps.find_comment.outputs.comment-id }}
          issue-number: ${{ github.event.pull_request.number }}
          body-path: audit_comment.md
          edit-mode: replace

      - name: Fail if vulnerabilities found
        if: steps.audit.outputs.has_vulnerabilities == 'true'
        run: |
          echo "::error::Security audit found vulnerabilities that must be addressed"
          exit 1
